// Generated by Claude Sonnet 5. Not hand-written by the project author.

import { discoverTestFiles } from '../discover';

// The official benchmark scripts live flat under test/benchmark, with no
// subdirectories to exclude -- unlike discoverTestFiles's default behavior
// for the correctness suite, we want every file here.
export function discoverBenchmarks(root: string): Promise<string[]> {
  return discoverTestFiles(root, { excludedDirs: new Set() });
}
