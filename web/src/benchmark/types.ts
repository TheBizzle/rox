// Generated by Claude Sonnet 5. Not hand-written by the project author.

export interface BenchmarkResult {
  name: string;
  path: string;
  wasmTimingsMs: number[];
  /** null if the native binary wasn't found, or a run failed. */
  nativeTimingsMs: number[] | null;
  /**
   * The last numeric line the benchmark script itself printed -- these
   * scripts call the language's own `clock()` builtin and print the
   * elapsed seconds, so this is the VM's self-reported compute time,
   * distinct from the wall-clock time measured around the whole call
   * from Node (which also includes call/startup overhead).
   */
  reportedVmSeconds: number | null;
}
